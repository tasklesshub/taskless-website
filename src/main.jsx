import React from "react";
import ReactDOM from "react-dom/client";
import TasklessLanding from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TasklessLanding />
  </React.StrictMode>
);
